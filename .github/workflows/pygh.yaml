name: pygh
on: workflow_dispatch

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v3
      - name: execute
        uses: ./.github/actions/pygh
        with:
          token: ${{ secrets.TOKEN }}